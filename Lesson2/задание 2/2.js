'use strict';

let a = 2; //присвоили переменной число 2
let x = 1 + (a *= 2);   // в этом выражении сначало переменну "а" умнажаем на 2 и присваиваем это значение в переменную "а"
                        // а затем делаем операцию прибавление и получаем 5, а это значение присваиваем в переменную "х". 